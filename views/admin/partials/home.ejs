<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <% if (!data[0].firstName || data[0].firstName.length===0) { %>
                <h3 class="title">Welcome <%= data[0].username %> !</h3>
                <% } else{%>
                <h3 class="title">Welcome <%= data[0].firstName %> !</h3>
                <% } %>

            </div>
            <div class="card-body">

            </div>
        </div>
    </div>
</div>